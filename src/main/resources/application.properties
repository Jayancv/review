# Provider - jpa is default
coupon.provider=jpa

# Test data - off by default
coupon.init-data=false

# SQL init - off by default, overridden per profile
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true